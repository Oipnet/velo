class GMap {
    constructor(target) {
        // Selector de la map
        this.target = target;
    }
    initMap() {
        // Initialisation de la map
    }
    createMarker(station){
        
    }
}

var map = new GMap('#gmap');
